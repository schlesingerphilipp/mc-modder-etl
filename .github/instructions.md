Follow these instructions
1. Read the Makefile and use the commands of it to install and run tests
2. Use the virtual environment ".venv" when directly calling python
3. Only directly call python directly, if there is no propper make method in the MAkefile
4. If you have to add dependencies add them to the pyproject.toml